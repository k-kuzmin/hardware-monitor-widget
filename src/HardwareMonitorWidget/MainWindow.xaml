<Window x:Class="HardwareMonitorWidget.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Hardware Monitor Widget"
        Height="420"
        Width="560"
        MinHeight="360"
        MinWidth="500"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Hardware Monitor" FontSize="20" FontWeight="SemiBold" />
            <TextBlock Text="{Binding GpuDisplayName}" Opacity="0.8" />
            <TextBlock Text="{Binding StartupStatus}" Opacity="0.7" FontSize="12" />
        </StackPanel>

        <ItemsControl Grid.Row="1" Margin="0,8,0,0" ItemsSource="{Binding Metrics}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,0,10" Padding="10" BorderThickness="1" CornerRadius="8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding Label}" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="1" Text="{Binding DisplayValue}" FontWeight="SemiBold" Margin="12,0,0,0" />

                            <ProgressBar Grid.Row="1"
                                         Grid.ColumnSpan="2"
                                         Margin="0,8,0,0"
                                         Height="16"
                                         Minimum="0"
                                         Maximum="100"
                                         Value="{Binding Value}"
                                         Foreground="{Binding BarBrush}" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
